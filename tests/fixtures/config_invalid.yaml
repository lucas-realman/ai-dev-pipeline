# ============================================================
# 测试用非法配置 — config_invalid.yaml
# 缺少必填项, 应触发 ConfigSchemaError
# ============================================================

# 缺少 orchestrator 段 (必填)

project:
  name: invalid-project

# 缺少 llm 段 (必填)

machines: []

# 缺少 task 段 (必填)
